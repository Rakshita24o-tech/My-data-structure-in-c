#include <stdio.h>
int main() {
int a[] = {5, 9, 2, 7, 3};
int n = sizeof(a)/sizeof(a[0]);
int index = 3; // 0-based
if (index >= 0 && index < n) {
int val = a[index];
printf("a[%d] = %d\n", index, val);
} else {
printf("Index out of range\n");
}
return 0;
}
